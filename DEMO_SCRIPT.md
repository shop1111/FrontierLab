# FrontierLab 三分钟演示脚本

演示全程使用仓库内预生成固件，不改代码、不联网。提前生成
`_build/acceptance/playground.html` 并用浏览器打开。

## 0:00–0:25 问题

画面：README 的闭环图。

念白：

> AI 能很快生成“看起来正确”的算法过程，但最终答案错误时，我们通常只能读
> 日志猜原因。FrontierLab 把过程记录成稳定的语义轨迹，再回答四个问题：输入
> 合法吗、契约在哪里失败、第一次和正确过程不同在哪里、能否给出聚焦反例切片。

## 0:25–1:20 一键定位

画面：AI Trace Clinic 默认 Faulty selection sort，点击 **Run diagnosis**。

念白：

> 默认案例是一段错误选择排序。一次点击后，页面自动跳到 step 10。参考过程
> 应该交换值 4 和 3，对应 item-2 与 item-4；实际过程用了过期下标，交换了值
> 4 和 5，对应 item-2 与 item-0。下方同时给出稳定实体变化，契约进一步指出
> 最终序列不再有序。

画面：依次指向 Schema、Contract failure、First divergence、Focus 四个摘要，
再指向 Expected/Actual 事件和两类差异证据。

## 1:20–1:50 可交付证据

画面：点击下载聚焦反例切片、下载诊断 HTML、复制修复提示。

念白：

> 诊断结果不是只能观看的页面。聚焦反例切片可以交给测试或 Agent，离线 HTML 可以
> 归档给评委，修复提示直接描述期望事件、实际事件和变化实体。错误会在页面内
> 显示，不依赖开发者控制台。

## 1:50–2:15 无误报与无参考轨迹

画面：切换 Correct selection sort 并运行，再切换 Custom 保持 expected 为空。

念白：

> 正确案例通过契约且没有首次分歧，证明这不是固定报错。现实中只有 actual
> 轨迹时，Clinic 仍执行 Schema 和契约检查，但会明确标记首次分歧已跳过，而不
> 伪造参考答案。

## 2:15–2:45 CLI 与独立消费者

画面：终端运行验收文档中的 `diagnose` 命令，展示退出码 2、step 10 和生成文件；
随后展示消费者 `moon tree` 中的 `shop1111/frontierlab@0.6.0`。

念白：

> 同一份固件可由 CLI 自动验收：有效轨迹有语义错误返回 2，调用错误返回 1，
> 完全通过返回 0。仓库中的消费者是独立 MoonBit 模块，只依赖 Mooncakes 已发布
> 的 v0.6.0，没有本地 override，证明闭环不是靠当前源码的私有实现拼出来的。

## 2:45–3:00 收束

画面：回到 README 闭环图与质量门禁摘要。

念白：

> FrontierLab 的核心价值不是再增加一个算法，而是把已有能力凝结成可理解、
> 可亲手验证、可交付给下一环节的诊断闭环：错误轨迹、首次分歧、聚焦反例切片、
> 修复提示。
